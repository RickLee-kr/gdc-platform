/** Mirrors backend `DEFAULT_GENERIC_HTTP_COMMON_HEADERS` (`app/connectors/router.py`). */

export const DEFAULT_GENERIC_HTTP_COMMON_HEADERS: Record<string, string> = {
  Accept: 'application/json, text/plain, */*',
  'Content-Type': 'application/json',
}
