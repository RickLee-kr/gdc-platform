/** Fallback sample for delivery prefix preview when no mapped event is available. */
export const DELIVERY_PREVIEW_SAMPLE_EVENT: Record<string, unknown> = {
  event_type: 'reconn',
  event_name: 'login',
  vendor: 'acme',
  product: 'edr',
}
