# Legacy Design Documents Archive

This directory holds superseded Source of Truth design documents moved during SoT replacement.

## 2026-06-14 replacement

No prior versions of the ten charter/guardrail documents existed in the repository before this replacement. Files were copied from `docs/source-of-truth/_incoming/` to `docs/source-of-truth/` as the new canonical set.

If future replacements occur, move the previous `docs/source-of-truth/*.txt` files here before installing the new set.
