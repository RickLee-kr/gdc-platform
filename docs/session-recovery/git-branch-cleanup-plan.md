# Git Branch Cleanup Plan — `feature/next-work`

Analysis-only document. **No source files are modified.** Only this plan file is added.

References used:

- `.specify/memory/constitution.md`
- `.specify/specs-index.md`
- `docs/v1-readiness-checklist.md`
- `docs/testing/e2e-functional-regression-matrix.md`

Buckets:

- **A** — v1-ready, safe to commit in small focused commits.
- **B** — needs explicit review before committing on this branch (touches admin-bootstrap contract, DB migrations, runtime preview semantics, or in-progress import lifecycle).
- **C** — experimental; out of v1 scope. Must not be committed to a v1 branch unless explicitly approved.

Generated by inspecting `git status --short`, `git diff --name-only`, `git diff --stat`, file contents, and dependencies on new untracked modules.

---

## 1. Repository snapshot

| Item | Value |
|------|-------|
| Branch | `feature/next-work` |
| HEAD | `7524c22 Add checkpoint extraction suggestions` |
| Modified (tracked) | 74 files |
| Untracked | 177 files |
| Deleted | 1 file (`frontend/test-results/observability-hard-reload-cache-disabled.png`) |

Recent commits already on the branch:

```
7524c22 Add checkpoint extraction suggestions
bcf09c8 Improve stream incremental fetch configuration UX
15e05cf Add high-scale runtime analytics and UI virtualization
b484240 Add runtime snapshot read model and snapshot-backed analytics
c276acf Add operational snapshot runtime read path
4bfbba9 Add unified dev platform bootstrap
d50f12f Handle password change required in dev validation
14d06ce Handle dev admin credential drift
d76f1ee Restore visible dev E2E fixtures
51263d6 Fix audit log test database stack
50e6a7f Add audit logs MVP
632c530 Fix runtime topology TypeScript build
8ba2886 Add runtime topology view MVP
5a81b55 Add runtime topology view MVP
20312d4 Add runtime pipeline debugger MVP
```

---

## 2. Already-committed work (step 5)

| Commit | Title | Bucket | v1-safe? | Follow-up needed | Stay in branch |
|---|---|---|---|---|---|
| `7524c22` | Add checkpoint extraction suggestions | A | Yes — backs `docs/v1-readiness-checklist.md` §5 (Checkpoint click flow). | Confirm `record-selection-workspace.tsx` still loads after split. | **Keep.** |
| `bcf09c8` | Improve stream incremental fetch configuration UX | A | Yes — Stream HTTP Request UX, scoped, isolated files (`incremental-fetch-*`). | None. | **Keep.** |
| `15e05cf` | Add high-scale runtime analytics and UI virtualization | A (incumbent) | Pushed/historical; aligns with Runtime visibility scope (`docs/v1-readiness-checklist.md` §3, §4.11). | None (already in production-track). | **Keep.** |
| `b484240` | Add runtime snapshot read model and snapshot-backed analytics | A (incumbent) | Yes — runtime visibility. | None. | **Keep.** |
| `c276acf` | Add operational snapshot runtime read path | A (incumbent) | Yes. | None. | **Keep.** |
| `4bfbba9` | Add unified dev platform bootstrap | A (incumbent) | Yes — dev infra only. | None. | **Keep.** |
| `d50f12f` / `14d06ce` | Dev validation password / credential drift handling | A (incumbent) | Yes — dev validation only, respects bootstrap contract. | None. | **Keep.** |
| `d76f1ee` | Restore visible dev E2E fixtures | A (incumbent) | Yes — dev/test only. | None. | **Keep.** |
| `51263d6` / `50e6a7f` | Audit logs MVP + test DB fix | A (incumbent) | Yes — additive. | None. | **Keep.** |
| `632c530` / `8ba2886` / `5a81b55` | Runtime topology view MVP + TS fix | A (incumbent) | Yes — read-only `/runtime/topology` per `specs/046`. Already shipped. | None. | **Keep.** |
| `20312d4` | Runtime pipeline debugger MVP | A (incumbent) | Yes — read-only `/runtime/streams/{id}/pipeline-debug` per `specs/047`. | None. | **Keep.** |

No commit on this branch is recommended for revert.

---

## 3. Dangerous working-tree items (step 6)

| Path | Risk surface | Why dangerous |
|---|---|---|
| `app/db/seed.py` (M) | Seed / admin password behavior | Rewrites bootstrap admin reconciliation logic to delegate to a new `app.platform_admin.admin_password_policy` module. Touches the Immutable Bootstrap Admin Credential Contract. |
| `app/platform_admin/admin_password_policy.py` (??) | Seed / admin password behavior | New centralized password policy module the seed change depends on. |
| `scripts/admin/reset-admin-password.sh` (M) | Install/bootstrap behavior | Rewrites the operator recovery script path, adds `GDC_RECONCILE_ADMIN_PASSWORD=true` flow. Production-data-safety adjacent. |
| `tests/test_seed_data.py` (M), `tests/test_dev_bootstrap_contract.py` (M) | Seed contract tests | Encode the new policy module behavior; would fail unless `admin_password_policy.py` is also committed. |
| `alembic/env.py` (M), `alembic/versions/20260523_0026_template_drafts.py` (??) | DB migration | Adds a brand-new `template_drafts` table (and FK index). Hard to revert once applied to a real catalog. |
| `app/mappers/mapper.py` (M) | StreamRunner mapping output | Now depends on `mapping_rules.py` + `transform_engine.py` (both untracked C-bucket modules). Committing mapper without those breaks runtime; committing both pulls all of spec 050 into v1. |
| `app/enrichers/enrichment_engine.py` (M) | StreamRunner enrichment output | Now depends on `computed_enrichment.py`, `enrichment_conditions.py`, `enrichment_spec.py`, `tenant_resolver.py` (all C-bucket). Same risk as mapper. |
| `app/runtime/preview_service.py` (M, +493 lines) | Runtime preview / mapping/extraction parity | Pulls in governance, semantic, mapping-pack, normalization, stellar suggestion paths. Drift between preview and StreamRunner is a `docs/v1-readiness-checklist.md` §9 release blocker. |
| `app/runtime/router.py` (M), `app/runtime/schemas.py` (M, +339 lines) | Runtime API surface | Exposes new pipeline-stages / governance / mapping suggestions / template export endpoints; locks Phase 2 surface into the v1 API. |
| `app/runtime/control_service.py` (M) | StreamRunner control path | Adds `MappingPathValidationError` enforcement on mapping save — desirable (A) but must be reviewed for backwards-compat on existing rows. |
| `app/runtime/pipeline_debug_service.py` (M) | Pipeline debug semantics | Drops `str(v)` coercion of `field_mappings_json`. Coupled with spec 050 dict-shape mapping. |
| `app/backup/router.py` (M), `app/backup/schemas.py` (M), `app/backup/postman_parser.py` (??) | Backup/import lifecycle | New Postman parse endpoint; needs API-test certification before v1. |
| `app/templates/router.py` (M) + `app/templates/draft_*` + `app/templates/models.py` + `app/templates/inference/*` | Template registry / runtime | New Template Drafts CRUD + filesystem storage and inference engine. Spec 049 says “spec/design only unless noted in implementation phases.” |
| `scripts/dev/bootstrap-dev-platform.sh` (M) | Install/bootstrap behavior | Adds Playwright smoke credential guidance. Does not overwrite admin password but extends operator messaging. Low risk; safe with the Playwright stabilization bundle. |
| `scripts/dev/generate-runtime-ui-scale-fixture.sh` (M) + `frontend/public/dev-fixtures/*` + `scripts/dev/fixtures/*` | Dev tooling / runtime fixture mode | Couples the runtime UI to a dev-only fixture mode (`GDC_RUNTIME_FIXTURE_MODE`) that gates fetches in `operationalSnapshot.ts` and `gdcRoutes.ts`. Adds dev branches into shared production code paths — needs review. |

---

## 4. UI complexity risks (step 7)

Frontend surfaces exposing the disallowed v1 terms (`dataset`, `workspace`, `pipeline`, `governance`, `semantic`, `intelligence`). **All are bucket C** and must not enter v1 unless explicitly approved.

| Path | Term(s) |
|---|---|
| `frontend/src/components/mappings/dataset-detection-panel.tsx` | dataset |
| `frontend/src/components/mappings/governance-summary-panel.tsx`, `governance-workspace-bar.tsx` | governance, workspace |
| `frontend/src/components/mappings/semantic-badge.tsx`, `semantic-insight-panel.tsx` | semantic |
| `frontend/src/components/mappings/pipeline-flow-visualization.tsx`, `pipeline-quality-panel.tsx`, `pipeline-stage-graph.tsx`, `pipeline-stage-preview-panel.tsx`, `flow-insights-panel.tsx`, `enrichment-pipeline-section.tsx` | pipeline |
| `frontend/src/components/mappings/telemetry-pipeline-workspace.tsx`, `wizard-pipeline-workspace.tsx`, `wizard-pipeline-adapters.ts`, `workspace-operational-status.tsx`, `workspace-stage-nav.tsx`, `workspace-types.ts`, `enrichment-workspace.tsx` | pipeline, workspace |
| `frontend/src/components/mappings/mapping-pack-apply-panel.tsx`, `mapping-template-panel.tsx`, `normalization-policy-apply-panel.tsx`, `mapping-coverage-panel.tsx`, `mapping-transform-chips.tsx`, `mapping-transform-editor.tsx`, `transform-drawer.tsx`, `transform-recommendation-chips.tsx`, `stage-diff-preview.tsx`, `final-output-contract-viewer.tsx`, `confidence-badge.tsx`, `target-schema-preset-select.tsx`, `stream-readiness-badge.tsx`, `stellar-field-info-panel.tsx`, `stellar-mapping-suggestions-panel.tsx` | advanced mapping packs / transform editor / governance / semantic intelligence |
| `frontend/src/components/streams/wizard/step-mapping.tsx` (M) | now a thin shell around `WizardPipelineWorkspace` |
| `frontend/src/components/streams/wizard/step-enrichment.tsx` (M) | now a thin shell around `WizardPipelineWorkspace` |
| `frontend/src/components/streams/wizard/step-review.tsx` (M) | adds `GovernanceSummaryPanel` + `StreamReadinessBadge` |
| `frontend/src/components/streams/wizard/wizard-state.ts` (M) | wizard subtitles renamed to “Dataset detection”, “governance” |
| `frontend/src/components/streams/stream-mapping-page.tsx` (M) | switches to `TelemetryPipelineWorkspace` |
| `frontend/src/components/streams/stream-mapping-model.ts` (M) | adds `transforms` + `intelligenceMeta` |
| `frontend/src/components/streams/stream-runtime-detail-page.tsx` (M) | adds `StreamReadinessBadge` |
| `frontend/src/components/streams/streams-console.tsx` (M) | adds `StreamReadinessBadge` |
| `frontend/src/components/streams/mapping-json-tree.tsx` (M) | pulls in `SemanticBadge` + checkpoint highlight (mixed; see §6) |
| `frontend/src/components/mappings/mapping-edit-page.tsx` (M) | switches to `TelemetryPipelineWorkspace` |
| `frontend/src/components/mappings/mapping-workspace.tsx` (M) | adds preview/runtime path formatting (A-relevant) but mixes with pipeline edits |
| `frontend/src/components/mappings/mapping-builder-table.tsx` (M) | pulls in `TransformDrawer`, `MappingTransformSummary`, `TransformRecommendationChips`, semantic detection |
| `frontend/src/components/templates/template-drafts-panel.tsx`, `template-draft-preview-modal.tsx` | template draft surface (B) |
| `frontend/src/components/runtime/runtime-fixture-mode-banner.tsx`, `runtime-overview-fixture-mode.test.tsx` | dev runtime fixture mode (B) |
| `frontend/src/hooks/useDatasetMappingIntelligence.ts`, `useStellarMappingSuggestions.ts`, `useStreamGovernance.ts` | dataset / governance / intelligence |
| `frontend/src/utils/datasetMappingIntelligence.test.ts`, `mappingIntelligenceMeta.{ts,test.ts}`, `mappingTransforms.ts`, `pipelineFlowInsights.ts`, `semanticAliasRegistry.ts`, `semanticDetection.{ts,test.ts}`, `semanticTransformRecommendations.ts`, `semanticTypes.ts`, `streamReadiness.{ts,test.ts}`, `targetSchemaPreset.ts`, `eventObjectDiff.{ts,test.ts}` | dataset / pipeline / semantic / intelligence |

---

## 5. File-by-file classification

Status legend: `M` modified, `D` deleted, `??` untracked. Action legend: **commit-A1/A2/A3** = grouped A commits (see §6); **B-hold** = leave uncommitted for review; **C-isolate** = move to separate stash/branch; **leave** = leave as-is; **revert** = revert after confirmation.

### 5.1 Bucket A — v1-ready / keep

#### A1. Record Selection / extraction path validation (v1 §4.5, §5)

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `app/parsers/extraction_paths.py` | New `validate_mapping_paths_for_extraction()` and path normalization used by control_service / preview_service / frontend tests. Required by §5 of v1 checklist. | commit-A1 |
| ?? | `tests/test_extraction_path_contract.py` | Direct evidence required by `docs/v1-readiness-checklist.md` §10. | commit-A1 |
| ?? | `tests/test_mapping_path_save_validation.py` | Verifies envelope-relative path rejection on save. | commit-A1 |
| M | `tests/test_event_extraction_event_root.py` | Adds `pytestmark = pytest.mark.functional_regression` — aligns with `pytest.ini`. | commit-A1 |
| M | `app/runtime/control_service.py` | Adds `MappingPathValidationError` and `_assert_mapping_paths_relative_to_extracted_event`. v1 §9 (mapping path drift blocker). Review fact: emits HTTP 422 `ENVELOPE_RELATIVE_MAPPING_PATH`. | commit-A1 |
| M | `app/runtime/pipeline_debug_service.py` | Drops `str(v)` cast for `field_mappings_json`. Tightly coupled to mapper (C) dict shape; safe only after dependent mapper changes are committed or after a small protective change keeps both shapes. **Hold until mapper bucket is resolved.** | B-hold |
| ?? | `frontend/src/utils/eventExtractionPaths.ts` | Frontend parity of `extract_events`; cited in v1 checklist §5 (`eventExtractionPaths.test.ts`). | commit-A1 |
| ?? | `frontend/src/utils/eventExtractionPaths.test.ts` | Companion tests. | commit-A1 |
| ?? | `frontend/src/utils/mappingPathValidation.ts` | Implements `isEnvelopeRelativeMappingPath` + message; consumed by `mappingValidation.ts`. | commit-A1 |
| ?? | `frontend/src/utils/mappingPathValidation.test.ts` | Companion tests. | commit-A1 |
| ?? | `frontend/src/utils/recordSelectionPaths.ts` + `.test.ts` | Record selection helpers consumed by `record-selection-workspace.tsx`. | commit-A1 |
| M | `frontend/src/utils/mappingValidation.ts` | Adds blocking `ENVELOPE_RELATIVE_MAPPING_PATH` warning + re-exports `fieldMappingsFromRows` from `mappingFieldMappings.ts`. Depends on new `mappingPathValidation.ts` and `mappingFieldMappings.ts`. | commit-A1 |
| M | `frontend/src/utils/mappingValidation.test.ts` | Covers the new envelope-relative warning. | commit-A1 |
| ?? | `frontend/src/utils/mappingFieldMappings.ts` | Extracted `fieldMappingsFromRows` helper (imported by `mappingValidation.ts`). | commit-A1 |
| M | `frontend/src/utils/mappingSourceSample.ts` | Tiny change supporting wrapping the sample doc for the extracted-event preview. | commit-A1 |
| M | `frontend/src/components/streams/wizard/wizard-json-extract.ts` | Adds `toExtractedEventRelativePath` (Record Selection path mapping). | commit-A1 |
| M | `frontend/src/components/streams/wizard/wizard-json-extract.test.ts` | Covers `toExtractedEventRelativePath`. | commit-A1 |
| M | `pytest.ini` | Adds `functional_regression` marker. | commit-A1 |

#### A2. Playwright smoke stabilization + functional regression matrix + v1 readiness

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `docs/v1-readiness-checklist.md` | This release-scoping document. | commit-A2 |
| ?? | `docs/testing/e2e-functional-regression-matrix.md` | Cited by the checklist. | commit-A2 |
| ?? | `scripts/testing/run-functional-regression.sh` | Entry point listed in the matrix. | commit-A2 |
| ?? | `tests/functional_regression_helpers.py` | Helper module for the runtime regression. | commit-A2 |
| ?? | `tests/test_functional_regression_extraction_e2e.py` | Cited in checklist §10. | commit-A2 |
| ?? | `tests/test_functional_regression_stream_runner.py` | Companion runtime E2E. | commit-A2 |
| ?? | `tests/wiremock/mappings/template-functional-regression-records.json` | WireMock fixture for nested envelope. | commit-A2 |
| ?? | `tests/wiremock/mappings/template-functional-regression-root-array.json` | WireMock fixture for root array. | commit-A2 |
| M | `frontend/e2e/operator-auth-runtime-smoke.spec.ts` | Replaces `unavailable` skip with a discriminated `formatProbeSkipReason` — matches matrix doc. | commit-A2 |
| M | `frontend/e2e/helpers/auth-flow.ts` | Adds discriminated `AuthProbeResult` and `signInForSmoke`. | commit-A2 |
| ?? | `frontend/e2e/record-selection-smoke.spec.ts` | Smoke spec listed in matrix. | commit-A2 |
| ?? | `frontend/e2e/record-selection-workspace-validation.spec.ts` | Extended validation spec cited in matrix. | commit-A2 |
| ?? | `frontend/playwright.config.smoke.ts` | Smoke config (port 4173). | commit-A2 |
| ?? | `frontend/playwright.config.validation.ts` | Validation config. | commit-A2 |
| ?? | `frontend/scripts/validate-playwright-smoke-env.mjs` | Preflight cited in matrix. | commit-A2 |
| ?? | `frontend/scripts/lib/playwright-smoke-env.mjs` | Helper used by the preflight. | commit-A2 |
| ?? | `frontend/scripts/lib/playwright-smoke-env.test.mjs` | Helper tests (require vitest config below). | commit-A2 |
| M | `frontend/package.json` | Adds `validate:playwright-smoke`, `pretest:playwright-smoke`, `test:playwright-smoke`. | commit-A2 |
| M | `frontend/vitest.config.ts` | `include: 'scripts/**/*.test.mjs'` — required by the helper tests. | commit-A2 |
| M | `scripts/dev/bootstrap-dev-platform.sh` | Adds Playwright smoke credential guidance and recovery hint. Does not overwrite admin password. Aligned with matrix doc. | commit-A2 |

#### A3. Minimal HelpTooltip / contextual help (v1 §3, §4)

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `frontend/src/components/ui/help-tooltip.tsx` | Small read-only tooltip primitive. | commit-A3 |
| ?? | `frontend/src/components/ui/help-tooltip-copy.ts` | Static copy used across pages. | commit-A3 |
| ?? | `frontend/src/components/ui/help-tooltip.test.tsx` | Tooltip tests. | commit-A3 |
| ?? | `frontend/src/components/streams/no-checkpoint-warning.tsx` + `.test.tsx` | Inline checkpoint warning surfaced from `stream-edit-page.tsx`. | commit-A3 |
| M | `frontend/src/components/destinations/destinations-management-page.tsx` | Only adds `HelpTooltip` next to Type / Enabled labels. | commit-A3 |
| M | `frontend/src/components/logs/logs-explorer-page.tsx` | Only adds `HelpTooltip` next to Stage header. | commit-A3 |
| M | `frontend/src/components/runtime/runtime-overview-sections.tsx` | Only adds `HelpTooltip` next to route failure / retry headers. | commit-A3 |
| M | `frontend/src/components/routes/route-edit-page.tsx` | Adds optional `help` slot to `Field`. | commit-A3 |
| M | `frontend/src/components/streams/stream-enrichment-page.tsx` | Single `HelpTooltip` next to Override Policy header. | commit-A3 |
| M | `frontend/src/components/streams/stream-edit-page.tsx` | Adds `HelpTooltip` to checkpoint / request body fields and inlines `NoCheckpointWarning`. Verify no behavioral changes outside help/warning. | commit-A3 |

#### A4. Record Selection workspace tooltips + checkpoint suggestions UI (uses A1/A3)

| Status | Path | Reason | Action |
|---|---|---|---|
| M | `frontend/src/components/streams/wizard/record-selection-workspace.tsx` | Adds `HelpTooltip` on Event Source / Event Root / Runtime Extraction / Mapping Source Path summary cards and wires checkpoint suggestion handlers. Part of v1 §4.5 / §5. | commit-A4 (after A1 + A3) |
| ?? | `frontend/src/components/streams/wizard/record-selection-workspace.test.tsx` | Companion tests for the workspace. | commit-A4 |
| ?? | `frontend/src/components/streams/wizard/wizard-operational-samples.ts` | Operational sample payloads (Records + root array) listed in the smoke spec. | commit-A4 |
| ?? | `frontend/src/utils/clipboard.ts` | Small `copyTextToClipboard` helper consumed by record selection + extraction summary. | commit-A4 |
| D | `frontend/test-results/observability-hard-reload-cache-disabled.png` | Generated Playwright artifact (no source/test references). | commit-A4 (include deletion) |

#### A5. `mapping-json-tree.tsx` (mixed)

| Status | Path | Reason | Action |
|---|---|---|---|
| M | `frontend/src/components/streams/mapping-json-tree.tsx` | Adds `eventRootHighlightPath`, `checkpointHighlightPath`, `onUseCheckpointPath` (record selection — A) **and** `SemanticBadge` import (C). | Split: keep checkpoint + highlight diffs in commit-A4; **drop** the `SemanticBadge` import and call site (defer to C bundle). |

---

### 5.2 Bucket B — needs review before keep

#### B1. cURL / Postman import lifecycle

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `app/backup/postman_parser.py` | New parser feeding `/backup/postman/parse`. Per v1 §3 “only if currently working and tested.” | B-hold |
| M | `app/backup/router.py` | Adds Postman parse endpoint. | B-hold |
| M | `app/backup/schemas.py` | Adds `PostmanParseRequest/Response/Summary`. | B-hold |
| ?? | `tests/test_postman_import.py` | Postman parse coverage. Verify these pass under `run-functional-regression.sh` before promotion to A. | B-hold |
| ?? | `frontend/src/components/connectors/http-import-panel.tsx` + `.test.tsx` | Curl + Postman panels used by Connectors and Operations pages. | B-hold |
| ?? | `frontend/src/components/connectors/new-connector-wizard-page.import.test.tsx` | Import-flow tests for the wizard. | B-hold |
| M | `frontend/src/components/connectors/new-connector-wizard-page.tsx` | Wires Postman/cURL panels into the wizard. | B-hold |
| M | `frontend/src/components/connectors/connectors-overview-page.tsx` | “Import from cURL / Postman” toggle. | B-hold |
| M | `frontend/src/components/connectors/connectors-overview-page.test.tsx` | New import test. | B-hold |
| M | `frontend/src/components/operations/operations-backup-page.tsx` | Replaces inline cURL state with reusable panels (Postman included). | B-hold |
| M | `frontend/src/components/operations/operations-backup-page.test.tsx` | Adds import-section render assertions. | B-hold |
| M | `frontend/src/api/gdcBackup.ts` | Adds `postPostmanParse`, switches `postCurlParse` to `safeRequestJsonResult`. | B-hold |
| M | `frontend/src/api.ts` | Adds `parse_errors` handling in HTTP error formatter (used by Postman). | B-hold |
| ?? | `frontend/src/utils/httpImportDraft.ts` | Shared navigation helper for both panels. | B-hold |

#### B2. Template Draft / Template Registry work (specs/049 — design-only by default)

| Status | Path | Reason | Action |
|---|---|---|---|
| M | `alembic/env.py` | Imports `app.templates.models`. Blocks template-drafts migration from being detected unless committed. | B-hold |
| ?? | `alembic/versions/20260523_0026_template_drafts.py` | New `template_drafts` table migration. **Do not apply** without explicit operator approval; spec 049 is design-only. | B-hold (do not run) |
| M | `app/templates/router.py` | Adds Template Draft CRUD + inference preview endpoints. | B-hold |
| ?? | `app/templates/draft_schemas.py`, `draft_service.py`, `draft_storage.py`, `models.py`, `inference/*` | Template Draft backend implementation. | B-hold |
| ?? | `frontend/src/api/gdcTemplateDrafts.ts` | Frontend API client. | B-hold |
| ?? | `frontend/src/components/templates/template-drafts-panel.tsx` | Drafts list/table on Templates page. | B-hold |
| ?? | `frontend/src/components/templates/template-draft-preview-modal.tsx` + `.test.tsx` | Draft preview modal. | B-hold |
| M | `frontend/src/components/templates/templates-overview-page.tsx` | Adds Published/Drafts tabs. | B-hold |
| ?? | `tests/test_template_drafts.py` | Backend tests. | B-hold |
| ?? | `specs/049-template-registry/spec.md` | New spec (design only). Safe to keep but ties to the rest of the bundle. | B-hold |
| M | `docs/master-design.md` | Adds reference to specs/049. Safe but pairs with B2. | B-hold |
| M | `.specify/specs-index.md` | Adds spec 049 (B) **and** spec 050 (C). Split: keep 049 entry; defer 050 entry to C. | B-hold (re-author) |

#### B3. Runtime fixture mode (dev-only branch in shared code)

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `frontend/src/lib/runtime-operational-fixture-mode.ts` + `.test.ts` | DEV opt-in fixture loader. | B-hold |
| ?? | `frontend/src/components/runtime/runtime-fixture-mode-banner.tsx` | Banner shown on Runtime / Routes pages. | B-hold |
| ?? | `frontend/src/components/runtime/runtime-overview-fixture-mode.test.tsx` | Companion tests. | B-hold |
| M | `frontend/src/api/operationalSnapshot.ts` | Branches into fixture when DEV flag set. Touches a shared production code path. | B-hold |
| M | `frontend/src/api/operationalSnapshot.test.ts` | Mocks for fixture mode. | B-hold |
| M | `frontend/src/api/gdcRoutes.ts` | Same fixture branch on `fetchRoutesList`. | B-hold |
| ?? | `frontend/src/api/gdcRoutes.fixture.test.ts` | Companion fixture tests. | B-hold |
| M | `frontend/src/components/routes/routes-overview-page.tsx` | Mounts the banner. | B-hold |
| M | `frontend/src/components/runtime/runtime-overview-page.tsx` | Mounts the banner. | B-hold |
| ?? | `frontend/public/dev-fixtures/{README.md,manifest.json,runtime-operational-snapshot-320x120.json,runtime-operational-snapshot-test.json,runtime-ui-320x120-20260523T065351Z.json}` | Static fixture assets served by Vite. Large (~270 KB each). Confirm policy on shipping JSON fixtures in `public/`. | B-hold |
| ?? | `scripts/dev/fixtures/*.json` | Local archive (5 files). | B-hold |
| M | `scripts/dev/generate-runtime-ui-scale-fixture.sh` | Generates both archive + public fixtures. | B-hold |
| M | `docs/performance/runtime-ui-virtualization-phase-6_5.md` | Documents the new DEV fixture mode. | B-hold |
| ?? | `frontend/src/lib/runtime-stream-selectors.test.ts`, `snapshot-stabilize.test.ts`, `windowed-virtual-range.test.ts`, `frontend/src/hooks/use-deferred-mount.test.ts` | Test scaffolding around runtime virtualization (already-shipped commits `15e05cf` / `b484240`). Safe but belongs with B3 since they were added alongside fixture mode. | B-hold |

#### B4. Admin password recovery contract changes

| Status | Path | Reason | Action |
|---|---|---|---|
| M | `app/db/seed.py` | Refactors bootstrap admin handling into a new policy module. **Touches the Immutable Bootstrap Admin Credential Contract.** Requires explicit operator approval. | B-hold |
| ?? | `app/platform_admin/admin_password_policy.py` | New centralized module. | B-hold |
| M | `scripts/admin/reset-admin-password.sh` | Switches to `GDC_RECONCILE_ADMIN_PASSWORD=true` workflow; expands preserved-entity guarantees. | B-hold |
| ?? | `docs/operations/admin-password-reset.md` | New operator runbook for the new flow. | B-hold |
| M | `docs/deployment-readiness.md` | Updates reset flow text. | B-hold |
| M | `docs/dev/dev-platform-environment-contract.md` | Same. | B-hold |
| M | `docs/operations/migration-integrity-validation.md` | Updates seed CLI table. | B-hold |
| M | `tests/test_seed_data.py` | Encodes new policy field names (`password_reconcile`, `password_reconcile_reason`). | B-hold |
| M | `tests/test_dev_bootstrap_contract.py` | Asserts new script behavior. | B-hold |
| M | `.env.example` | Adds `GDC_RECONCILE_ADMIN_PASSWORD` commented entry. | B-hold |

---

### 5.3 Bucket C — experimental / out of v1 scope

#### C1. Spec 050 / pipeline workspace (mapping + enrichment evolution)

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `specs/050-pipeline-workspace/spec.md` | Spec for the new pipeline workspace (out of v1 scope per `docs/v1-readiness-checklist.md` §8). | C-isolate |
| ?? | `app/mappers/mapping_rules.py`, `transform_engine.py`, `transform_normalize.py`, `mapping_templates.py`, `mapping_intelligence_scoring.py`, `mapping_conflict_detection.py`, `mapping_coverage.py`, `mapping_packs/` (+ `__init__`, `aws_vpc_flow_logs_stellar.py`, `cloudtrail_stellar.py`, `okta_system_logs_stellar.py`), `normalization_policies.py`, `normalization_policy_apply.py`, `pipeline_explanations.py`, `pipeline_quality_analysis.py`, `semantic_alias_registry.py`, `semantic_detection.py`, `semantic_metadata.py`, `semantic_types.py`, `source_profile_detection.py`, `stellar_mapping_suggestions.py`, `stellar_metadata_catalog.py`, `stream_readiness_evaluation.py`, `dataset_mapping_intelligence.py`, `governance_snapshot.py`, `data/stellar_metadata_catalog.json` | Implementation of dataset / governance / semantic / mapping packs / transform editor / large pipeline visualization / advanced mapping intelligence. All terms forbidden by §7. | C-isolate |
| ?? | `app/enrichers/computed_args.py`, `computed_enrichment.py`, `enrichment_conditions.py`, `enrichment_spec.py`, `tenant_resolver.py`, `vendor_profile.py` | Spec 050 enrichment side. | C-isolate |
| ?? | `app/runtime/pipeline_preview.py` | Pipeline-stages preview implementation. | C-isolate |
| M | `app/mappers/mapper.py` | StreamRunner mapper now depends on `mapping_rules.py` + `transform_engine.py`. **Cannot be committed without those modules.** | C-isolate (revert pending) |
| M | `app/enrichers/enrichment_engine.py` | StreamRunner enrichment depends on `enrichment_spec.py` + `computed_enrichment.py` + `enrichment_conditions.py` + `tenant_resolver.py`. | C-isolate (revert pending) |
| M | `app/runtime/preview_service.py` | +493 lines wiring `run_mapping_suggestions`, `run_dataset_mapping_intelligence`, `run_pipeline_stages_preview`, `run_stream_governance_evaluation`, etc. | C-isolate (revert pending) |
| M | `app/runtime/router.py` | Exposes pipeline-stages / governance / suggestions / template export endpoints. | C-isolate (revert pending) |
| M | `app/runtime/schemas.py` | +339 lines for `PipelineStagesPreview*`, `StreamGovernanceEvaluation*`, `MappingSuggestions*`, `NormalizationPolicyApplyPreview*`, etc. | C-isolate (revert pending) |
| ?? | `scripts/extract_stellar_metadata_catalog.py` | Tooling for the Stellar catalog. | C-isolate |
| ?? | `tests/test_dataset_mapping_intelligence.py`, `test_normalization_governance.py`, `test_semantic_pipeline_intelligence.py`, `test_stellar_mapping_suggestions.py`, `test_stellar_metadata_catalog.py`, `test_stream_readiness_evaluation.py`, `test_transform_engine.py` | Backend tests for C1. | C-isolate |
| ?? | `frontend/src/components/mappings/{confidence-badge,dataset-detection-panel,enrichment-pipeline-section,enrichment-workspace,final-output-contract-viewer,flow-insights-panel,governance-summary-panel,governance-workspace-bar,mapping-coverage-panel,mapping-pack-apply-panel,mapping-template-panel,mapping-transform-chips,mapping-transform-editor,normalization-policy-apply-panel,pipeline-flow-visualization,pipeline-quality-panel,pipeline-stage-graph,pipeline-stage-preview-panel,semantic-badge,semantic-insight-panel,stage-diff-preview,stellar-field-info-panel,stellar-mapping-suggestions-panel,stream-readiness-badge,target-schema-preset-select,telemetry-pipeline-workspace,transform-drawer,transform-recommendation-chips,wizard-pipeline-adapters,wizard-pipeline-workspace,workspace-operational-status,workspace-stage-nav,workspace-types}.{ts,tsx}` | Pipeline workspace UI. Hits every §7 forbidden term. | C-isolate |
| ?? | `frontend/src/components/mappings/mapping-builder-table.test.tsx` | Tests for the C1 builder rebuild. | C-isolate |
| M | `frontend/src/components/mappings/mapping-builder-table.tsx` | Pulls in `TransformDrawer`, `MappingTransformSummary`, `TransformRecommendationChips`, semantic detection. | C-isolate (revert to current main behavior; reapply later) |
| M | `frontend/src/components/mappings/mapping-edit-page.tsx` | Switches to `TelemetryPipelineWorkspace`. | C-isolate (revert pending) |
| M | `frontend/src/components/mappings/mapping-workspace.tsx` | Mixed: A-relevant `formatPreviewSamplePath` + `formatRuntimeExtractionPath`, but also pipeline-stage adapters. **Split required** — keep only the path-formatting adjustments. | Split: keep A part, defer C part |
| M | `frontend/src/components/streams/wizard/step-mapping.tsx` | Replaced with `WizardPipelineWorkspace`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/wizard/step-enrichment.tsx` | Replaced with `WizardPipelineWorkspace`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/wizard/step-review.tsx` | Adds `GovernanceSummaryPanel`, `StreamReadinessBadge`, `useStreamGovernance`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/wizard/wizard-state.ts` | Subtitle rename to “Dataset detection” / “governance”. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/stream-mapping-page.tsx` | Switches to `TelemetryPipelineWorkspace`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/stream-mapping-model.ts` | Adds `transforms?: MappingTransformStep[]` + `intelligenceMeta`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/stream-runtime-detail-page.tsx` | Adds `StreamReadinessBadge`. | C-isolate (revert pending) |
| M | `frontend/src/components/streams/streams-console.tsx` | Adds `StreamReadinessBadge`. | C-isolate (revert pending) |
| ?? | `frontend/src/components/streams/mapping-json-tree.tsx` (C portion) | `SemanticBadge` import + render site. | C-isolate (revert that hunk only) |
| M | `frontend/src/api/gdcRuntimePreview.ts` | +370 lines of pipeline-stage / governance / suggestions / template-export types. | C-isolate (revert pending) |
| M | `frontend/src/api/gdcRuntimeUi.ts` | Adds `readiness?` + `governance_snapshot?` to mapping UI save response. | C-isolate (revert pending) |
| M | `frontend/src/api/streamRows.ts` | Adds `governanceReadiness` cache. | C-isolate (revert pending) |
| M | `frontend/src/api/types/gdcApi.ts` | Adds `StreamReadinessResultApi` + governance fields. | C-isolate (revert pending) |
| M | `frontend/src/hooks/useMappingPreview.ts` | Switches preview to `runPipelineStagesPreview`. | C-isolate (revert pending) |
| ?? | `frontend/src/hooks/useDatasetMappingIntelligence.ts`, `useStellarMappingSuggestions.ts`, `useStreamGovernance.ts` | C-bundle hooks. | C-isolate |
| ?? | `frontend/src/utils/datasetMappingIntelligence.test.ts`, `eventObjectDiff.{ts,test.ts}`, `mappingIntelligenceMeta.{ts,test.ts}`, `mappingTransforms.ts`, `pipelineFlowInsights.ts`, `semanticAliasRegistry.ts`, `semanticDetection.{ts,test.ts}`, `semanticTransformRecommendations.ts`, `semanticTypes.ts`, `streamReadiness.{ts,test.ts}`, `targetSchemaPreset.ts` | Pipeline workspace utilities. | C-isolate |

---

### 5.4 Leave-as-is / housekeeping

| Status | Path | Reason | Action |
|---|---|---|---|
| ?? | `docs/session-recovery/recent-commits.txt`, `recovery-commands.sh`, `session-recovery-20260521-1501.md`, `staged.patch`, `untracked-files.txt`, `working-tree.patch` | Pre-existing recovery snapshot artifacts. | Leave uncommitted. |
| ?? | `docs/session-recovery/git-branch-cleanup-plan.md` | This document. | commit-A2 (next to v1 checklist / matrix). |

---

## 6. Recommended next git actions (step 8)

Recommended order (one shell at a time; **no force resets**, **no `git clean`**):

1. **Document-only commit (this plan):**
   - `git add docs/session-recovery/git-branch-cleanup-plan.md`
   - `git commit -m "Add v1 branch cleanup classification plan"`

2. **commit-A1 — Record Selection / extraction path validation** (§5.1 A1):
   - Stage:
     - `app/parsers/extraction_paths.py`
     - `tests/test_extraction_path_contract.py`, `tests/test_mapping_path_save_validation.py`, `tests/test_event_extraction_event_root.py`
     - `app/runtime/control_service.py` (mapping-path validation only)
     - `frontend/src/utils/eventExtractionPaths.ts(.test.ts)`, `mappingPathValidation.ts(.test.ts)`, `recordSelectionPaths.ts(.test.ts)`, `mappingValidation.ts(.test.ts)`, `mappingFieldMappings.ts`, `mappingSourceSample.ts`
     - `frontend/src/components/streams/wizard/wizard-json-extract.ts(.test.ts)`
     - `pytest.ini`
   - Commit message: `Enforce extracted-event-relative mapping paths and contract tests`.

3. **commit-A2 — v1 readiness checklist + functional regression matrix + Playwright smoke stabilization** (§5.1 A2):
   - Stage:
     - `docs/v1-readiness-checklist.md`, `docs/testing/e2e-functional-regression-matrix.md`
     - `scripts/testing/run-functional-regression.sh`, `tests/functional_regression_helpers.py`, `tests/test_functional_regression_extraction_e2e.py`, `tests/test_functional_regression_stream_runner.py`, `tests/wiremock/mappings/template-functional-regression-*.json`
     - `frontend/e2e/operator-auth-runtime-smoke.spec.ts`, `frontend/e2e/helpers/auth-flow.ts`, `frontend/e2e/record-selection-smoke.spec.ts`, `frontend/e2e/record-selection-workspace-validation.spec.ts`
     - `frontend/playwright.config.smoke.ts`, `frontend/playwright.config.validation.ts`
     - `frontend/scripts/validate-playwright-smoke-env.mjs`, `frontend/scripts/lib/playwright-smoke-env.mjs(.test.mjs)`
     - `frontend/package.json`, `frontend/vitest.config.ts`
     - `scripts/dev/bootstrap-dev-platform.sh`
   - Commit message: `Stabilize Playwright smoke flow and ship v1 readiness docs`.

4. **commit-A3 — HelpTooltip primitive + minimal per-page help** (§5.1 A3):
   - Stage:
     - `frontend/src/components/ui/help-tooltip.tsx(.test.tsx)`, `help-tooltip-copy.ts`
     - `frontend/src/components/streams/no-checkpoint-warning.tsx(.test.tsx)`
     - `frontend/src/components/destinations/destinations-management-page.tsx`
     - `frontend/src/components/logs/logs-explorer-page.tsx`
     - `frontend/src/components/runtime/runtime-overview-sections.tsx`
     - `frontend/src/components/routes/route-edit-page.tsx`
     - `frontend/src/components/streams/stream-enrichment-page.tsx`
     - `frontend/src/components/streams/stream-edit-page.tsx`
   - Commit message: `Add HelpTooltip and inline NoCheckpointWarning on existing screens`.

5. **commit-A4 — Record Selection workspace tooltips + operational samples** (§5.1 A4 + A5 split):
   - Stage:
     - `frontend/src/components/streams/wizard/record-selection-workspace.tsx`, `.test.tsx`
     - `frontend/src/components/streams/wizard/wizard-operational-samples.ts`
     - `frontend/src/utils/clipboard.ts`
     - Edited `frontend/src/components/streams/mapping-json-tree.tsx` **without** the `SemanticBadge` import/usage hunk (revert that one hunk before staging).
     - `frontend/test-results/observability-hard-reload-cache-disabled.png` (deletion of generated artifact).
   - Commit message: `Add Record Selection workspace tooltips and operational samples`.

6. **Isolate experimental work — Bucket C (§5.3):**
   - Create a side branch from `feature/next-work` HEAD:
     - `git switch -c feature/pipeline-workspace-experimental`
   - Move every C item into that branch (commit as `WIP: pipeline workspace experimental snapshot` to preserve history; do **not** push to a v1 line).
   - Back on `feature/next-work`:
     - `git checkout -- app/mappers/mapper.py app/enrichers/enrichment_engine.py app/runtime/preview_service.py app/runtime/router.py app/runtime/schemas.py app/runtime/pipeline_debug_service.py app/mappers/mapper.py` (revert M-files belonging to C).
     - `git checkout -- frontend/src/components/streams/wizard/step-mapping.tsx step-enrichment.tsx step-review.tsx wizard-state.ts` (revert C portions).
     - `git checkout -- frontend/src/components/streams/stream-mapping-page.tsx stream-mapping-model.ts stream-runtime-detail-page.tsx streams-console.tsx`.
     - `git checkout -- frontend/src/components/mappings/mapping-builder-table.tsx mapping-edit-page.tsx` and the C-only hunks in `mapping-workspace.tsx` and `mapping-json-tree.tsx`.
     - `git checkout -- frontend/src/api/gdcRuntimePreview.ts gdcRuntimeUi.ts streamRows.ts types/gdcApi.ts`.
     - `git checkout -- frontend/src/hooks/useMappingPreview.ts`.
   - Untracked C files (`app/mappers/...`, `app/enrichers/...`, `frontend/src/components/mappings/...`, etc.) stay only on the experimental branch (do **not** delete them — see workspace rule `preserve-user-entities.mdc`).

7. **Leave Bucket B uncommitted on `feature/next-work` until explicit go-ahead:**
   - B1 (cURL/Postman import) — wait for API-test certification per v1 §6.
   - B2 (Template Drafts + migration) — requires operator approval. **Do not run** `alembic upgrade` against any non-test catalog until then.
   - B3 (runtime fixture mode) — review whether DEV-only branches are acceptable in shared production code paths.
   - B4 (admin password recovery) — requires constitution-level review. **Do not commit.**

8. **Validation expectations:**
   - After commits A1–A4: `cd frontend && npm run build` (per `frontend-build-after-edit.mdc`), `pytest -m functional_regression`, and `npm run test:playwright-smoke` should still pass.
   - After C isolation: run `pytest tests/test_mapping_pipeline.py tests/test_event_extraction_event_root.py` to confirm StreamRunner mapper/enrichment behavior on the v1 branch.

---

## 7. Summary counts

- Bucket A (commit in 4 small commits): **34 files** (5 modified + 28 untracked + 1 deleted).
- Bucket B (hold for review): **49 files** (15 modified + 34 untracked; admin/migration/import/fixture/template).
- Bucket C (isolate to experimental branch): **148 files** (24 modified hunks need revert; 124 untracked stay on the side branch).
- Leave as-is: **6 files** (existing `docs/session-recovery/*` artifacts).

Total accounted: 237 file paths (after deduping the M+?? overlaps across buckets where the same file has both a modified hunk and untracked siblings).

---

## 8. Top 5 riskiest files

1. `app/db/seed.py` (M) — bootstrap admin contract; B4 hold.
2. `alembic/versions/20260523_0026_template_drafts.py` (??) — DB migration; B2 hold (never run on non-test catalog without operator approval).
3. `app/runtime/preview_service.py` (M, +493) — runtime/preview parity blocker if drift exists; C revert.
4. `app/mappers/mapper.py` (M) + `app/enrichers/enrichment_engine.py` (M) — StreamRunner runtime depends on C-bucket modules not yet committed; C revert to keep the v1 branch buildable.
5. `scripts/admin/reset-admin-password.sh` (M) + `app/platform_admin/admin_password_policy.py` (??) — operator recovery workflow change tied to the bootstrap admin contract; B4 hold.

## 9. Recommended next action

**Adopt the four A-commits above (A1 → A2 → A3 → A4), then isolate Bucket C onto `feature/pipeline-workspace-experimental`, then schedule explicit review meetings for B1 (cURL/Postman), B2 (Template Drafts + migration), B3 (runtime fixture mode), and B4 (admin password recovery).** Until those reviews land, leave the corresponding files uncommitted on `feature/next-work`; do not delete or revert them.
