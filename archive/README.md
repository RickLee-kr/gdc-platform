# Repository archives

Non-runtime artifacts moved out of active paths during cleanup.

| Path | Former location | Notes |
|------|-----------------|-------|
| `repo-backup/` | `.backup/` | One-off spec/constitution backups |
| `scripts-pending-merge-after-db-pr/` | `scripts/pending-merge-after-db-pr/` | DB stabilization PR merge notes and `.bak` files |

Do not delete without operator review. Runtime, auth, and compose files are not stored here.
