"""Auth use-cases — validate credentials and issue tokens."""

# TODO: Wire single admin account and JWT issuance (master design §21).


class AuthService:
    """Placeholder service for login lifecycle."""

    def authenticate(self, username: str, password: str) -> bool:
        """TODO: validate against stored admin credentials."""

        return False
